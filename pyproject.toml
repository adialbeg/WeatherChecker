[tool.poetry]
name = "weatherchecker"
version = "0.1.0"
description = ""
authors = ["Adi Albeg"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
streamlit = "^1.35.0"
pandas = "^2.2.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
